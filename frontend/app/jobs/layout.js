export const metadata = {
    title: "Jobs | Jobify",
    description: "Browse and apply to jobs.",
};
export default function JobsPage({children}) {
    return (
        <>
        {children}
        </>
    );
}
export const metadata = {
    title: "ATS Analysis | Jobify",
    description: "Analyze and optimize your resume for job applications.",
};
export default function ResumePage({children}) {
    return (
        <>
        {children}
        </>
    );
}